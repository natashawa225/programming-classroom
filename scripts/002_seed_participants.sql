-- Participant seeding now uses a generated SQL file plus a private CSV.
--
-- Run this from the repo root:
--   node scripts/generate-participant-seed.mjs
--
-- Generated outputs:
--   scripts/generated/participants_seed.sql
--   scripts/generated/participant-passwords.csv
--
-- Apply the generated SQL to Supabase as an admin.
-- Keep the CSV private; it contains the only plain-text copy of participant passwords.
